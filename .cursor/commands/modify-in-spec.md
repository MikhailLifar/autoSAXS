# modify-in-spec

For now DO NOT TOUCH A SIGNLE LINE OF CODE.
Instead go to `repos/docs`, choose there an appropriate spec file and modify the spec according to my query.
There are two apps in my project:
- SAXS processing pipeline. Entry point is `repos/pipeline.py`. The location of spec is `repos/docs/pipeline_interactive_spec.md`.
- SAXS processing GUI (guisaxs). Entry point is `guisaxs` (installed console script). The location of spec is `repos/docs/guisaxs_spec.md`.

There is also a spec on skill-oriented paradigm, on which `autosaxs` package, underlying both apps, is built. The location of spec is `repos/docs/skills_paradigm.md`.

Examine the spec carefully and modify all the sections related to my request.
IF SOMETHING IS UNCLEAR OR AMBIGUOUS IN MY INSTRUCTIONS, QUERY ME FOR CLARIFICATION after you finished to modify the Spec.
